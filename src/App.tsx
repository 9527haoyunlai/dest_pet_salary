export default function App() {
  return (
    <main className="app-shell">
      <section className="status-card" aria-labelledby="app-title">
        <p className="phase-label">Phase 1.5 · Desktop Shell Bootstrap</p>
        <h1 id="app-title">Salary Garden</h1>
        <p>Tauri + React is running</p>
      </section>
    </main>
  );
}

