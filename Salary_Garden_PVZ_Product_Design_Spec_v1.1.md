# Salary Garden / PVZ 工资花园
## 产品需求文档 + 技术设计文档 + Codex 实现上下文（SSOT）

> 文档版本：v1.1  
> 状态：已实现至 Phase 5C；下一阶段 Phase 6  
> 日期：2026-08-18  
> 目标技术栈：Tauri 2 + React + TypeScript + PixiJS + 本地 SQLite  
> 目标读者：Codex / 开发者 / 后续维护者  
> 文档职责：本文件是当前版本的**单一事实源（Single Source of Truth）**。实现时如代码、临时想法与本文冲突，以本文为准，除非用户后续明确修改产品决策。

---

# 0. 一句话定义

这是一个运行在桌面的“工资实时可视化 + 极轻量 PVZ 游戏化”应用：

- 后台根据**当前时间、工资周期、实际工作日和每日工作时段**精确计算“此刻理论上已经赚到的工资”；
- 前端用《植物大战僵尸》式草坪场景，把工作收入映射为由金盏花产生的银币、金币、钻石；
- 玩家可以手动点击货币，也可以由固定的磁吸菇自动收集；
- 实际收集到的钱进入独立的“游戏钱包”；
- 程序关闭、休眠或未启动期间遗漏的应得货币不会丢失，下次启动时被确定性重算并包装成一个“离线收益钱袋”，由用户点击领取；
- 同时保留一个完全独立、可重算、不会因为没启动程序而丢失的“真实工资账本”；
- 用户可以在 UI 中切换显示“真实工资”或“游戏收集钱包”。

本产品不是完整 PVZ，不做关卡、波次、胜负、复杂僵尸或植物系统。游戏部分只为工资可视化服务。

---

# 1. 已冻结的核心产品决策

以下内容视为当前版本的硬约束，Codex 不应擅自扩展或替换。

## 1.1 技术栈

| 层 | 选型 |
|---|---|
| 桌面壳 | Tauri 2 |
| 前端 UI | React |
| 语言 | TypeScript |
| 游戏/动画层 | PixiJS |
| 本地持久化 | SQLite |
| 后端/权威计算 | Tauri Rust 侧 |
| 网络后端 | MVP 不需要 |
| 资产 | 用户本地下载的 PVZ 资源包，通过资产清单映射 |

不要改成 Electron、Next.js、云后端或 Supabase。当前产品应是**本地优先、离线可用**的桌面应用。

## 1.2 工资周期

一个工资周期定义为：

> 从“上个月最后一个工作日之后的第一个工作日”开始，到“本月最后一个工作日”结束。

在当前规则下：

- 周六、周日永远不是工作日；
- 法定节假日不是工作日；
- MVP 不实现调休工作周末；
- 一个周期中的实际工作日数量记为 `N`；
- 月薪记为 `M`；
- 每个工作日的工资为 `M / N`。

工资周期应以用户配置的本地时区和工作日历为准，不要直接依赖 UTC 日期。

## 1.3 每日工作时间

每天有效计薪时间固定为：

| 时段 | 时间 | 时长 |
|---|---:|---:|
| 上午 | 08:40–11:40 | 3 小时 |
| 午休 | 11:40–13:30 | 不计薪 |
| 下午 | 13:30–17:30 | 4 小时 |
| 合计 | — | 7 小时 |

一天有效工作秒数：

`7 × 60 × 60 = 25,200 秒`

当前版本不要把午休算入工资，也不要在非工作时段生成工资货币。

## 1.4 三种工资货币

工资游戏化货币只有三种：

| 类型 | 出现条件 | 视觉等级 |
|---|---|---|
| 银币 | 每 10 个有效工作秒 | 普通 |
| 金币 | 每 60 个有效工作秒 | 稀有 |
| 钻石 | 每 3,600 个有效工作秒 | 高价值 |

存在覆盖规则：

1. 所有事件以“有效工作秒”而不是自然墙钟秒为基准；
2. 每到 10 秒边界最多只生成一个货币事件；
3. 如果该边界同时是 60 秒边界，则生成金币，不生成银币；
4. 如果该边界同时是 3,600 秒边界，则生成钻石，不生成金币和银币；
5. 因此钻石优先级 > 金币 > 银币。

完整一个有效工作小时的理论事件数固定为：

- 银币 300；
- 金币 59；
- 钻石 1；
- 总事件 360。

完整一个 7 小时工作日的理论事件数固定为：

- 银币 2,100；
- 金币 413；
- 钻石 7；
- 总事件 2,520。

## 1.5 两种用户可见收入口径，三个内部账本层

用户感知上有“两套钱包/收入系统”，内部实现必须拆成三层：

### A. 真实工资账本（Payroll Truth）

回答：

> “按照时间和工资规则，此刻理论上已经赚了多少钱？”

它是财务真值来源。

特点：

- 不依赖应用是否运行；
- 不依赖 setInterval 是否准时执行；
- 不通过不断 `+金额` 获得；
- 任何时刻都可以根据当前时间重新推导；
- 电脑休眠、程序退出、系统恢复后直接重算；
- UI 可以每 250ms / 500ms / 1s 刷新，但刷新频率不是账本本身。

### B. 货币应得账本（Reward Entitlement）

回答：

> “按照有效工作时间，到此刻理论上应该已经产生多少枚银币、金币、钻石？”

它同样是确定性可重算状态。

它用于：

- 生成实时掉落；
- 检测程序未运行期间遗漏的货币；
- 生成离线钱袋；
- 防止重复发放；
- 对账。

### C. 游戏收集钱包（Collected Wallet）

回答：

> “玩家真正点击或被磁吸菇收集了多少货币？”

只有发生“领取/收集”时才增加。

因此：

`真实工资 != 当前游戏钱包`

在任意时刻这是允许且正常的。

---

# 2. 产品体验目标

## 2.1 核心体验

用户打开桌面应用时，不是在看一个传统薪资表，而是在看一个持续运行的 PVZ 风格庭院：

- 左侧露出一部分房子；
- 中间是三行、每行约七格的草坪；
- 右侧露出一部分街道；
- 中路存在固定金盏花、固定磁吸菇、无限血坚果和桶装僵尸；
- 金盏花持续把工作时间转换为货币动画；
- 磁吸菇减少点击负担；
- 玩家可以用向日葵产生的阳光种豌豆射手；
- 豌豆射手攻击当前桶装僵尸；
- 僵尸死亡后，从右侧重新出现一个新的桶装僵尸继续攻击坚果；
- 坚果永远不会死亡；
- 游戏没有失败和通关。

目标不是“让用户玩 PVZ”，而是让工作过程变成一个持续可看的桌面小生态。

## 2.2 产品情绪

希望呈现：

- 原版 PVZ 式的轻松、夸张、弹性动画感；
- 钱币掉落带来即时收入反馈；
- 不要求用户频繁操作；
- 应用放在桌面一角也有观赏性；
- 退出应用不会产生“我亏了工资”的焦虑；
- 离线后回来时，通过钱袋获得“补发”的正向反馈。

---

# 3. 界面信息架构

## 3.1 完整模式

推荐默认窗口：

`960 × 540`

允许后续微调，但不要因为适配而破坏主要场景比例。

完整模式大致由以下区域构成：

1. 顶部：植物卡片 / 状态栏；
2. 中部：PVZ 草坪主场景；
3. 底部或角落：钱包信息；
4. 设置入口；
5. 当前工作状态的小型提示。

## 3.2 收起模式

推荐：

`300 × 120`

只显示关键收入信息，例如：

- 今日金额；
- 当前工资周期金额；
- 当前显示口径；
- 一个极小的金盏花/金币动画。

点击或双击后恢复完整场景。

此模式属于桌面效率功能，不改变任何账本计算。

## 3.3 钱包显示切换

设置中提供：

**显示口径**

- 实时工资；
- 游戏钱包。

无论使用哪种口径，都同时显示两个时间范围：

- 今日；
- 当前工资周期。

因此 UI 实际是一个二维矩阵：

| 显示口径 | 今日 | 当前工资周期 |
|---|---|---|
| 实时工资 | 今日理论已赚 | 周期理论已赚 |
| 游戏钱包 | 今日已收集 | 周期已收集 |

可额外以小字显示：

`待收集 +¥X.XX`

但待收集不是主钱包余额。

切换只改变显示，不改变底层状态。

---

# 4. 草坪与场景设计

## 4.1 背景构成

完整背景需要呈现：

`左侧房屋 → 中部草坪 → 右侧街道`

草坪：

- 3 行；
- 每行 7 格；
- 保持明显的 PVZ 棋盘感；
- 中间一行是主要玩法行；
- 上下两行主要用于构图、空间感以及有限种植，不新增其他战线规则。

如果资源包中存在 `sod3row` 一类现成三行草坪，优先作为基础背景。

## 4.2 中路固定布局

从左向右，推荐冻结为：

| 中路格子 | 内容 | 是否可移除 |
|---:|---|---|
| 1 | 金盏花 | 否 |
| 2 | 磁吸菇 | 否 |
| 3 | 可用/留白 | — |
| 4 | 无限血坚果 | 否 |
| 5–7 | 根据美术布局作为僵尸接近区/可用视觉区 | — |

桶装僵尸应停在坚果右侧并进行啃食动画。

金盏花位于坚果后方，因此始终处于安全状态。

## 4.3 固定对象

以下对象是场景机制，不是普通可种植物：

- 金盏花；
- 磁吸菇；
- 无限血坚果。

它们：

- 不消耗阳光；
- 不可铲除；
- 不可重复种植；
- 不被僵尸真正杀死；
- 不参与普通卡片冷却。

---

# 5. 顶部植物卡片

顶部只保留四张卡：

1. 金盏花；
2. 普通豌豆射手；
3. 向日葵；
4. 磁吸菇。

产品语义：

| 卡片 | 状态 |
|---|---|
| 金盏花 | 已部署，状态展示 |
| 磁吸菇 | 已部署，状态展示 |
| 向日葵 | 可种植 |
| 豌豆射手 | 可种植 |

金盏花和磁吸菇卡片应略微变暗，显示“已部署”或类似状态。

不要加入：

- 坚果卡；
- 其他植物；
- 铲子；
- 植物升级；
- 卡槽扩展；
- 商店；
- 冷却强化。

---

# 6. 轻量游戏玩法

## 6.1 阳光系统

阳光与工资完全隔离。

阳光只用于：

- 种向日葵；
- 种豌豆射手。

阳光不能：

- 兑换工资；
- 改变工资；
- 改变银币/金币/钻石生成速度；
- 进入工资钱包。

建议初始调参值（全部集中配置，后续可调）：

| 参数 | MVP 默认 |
|---|---:|
| 初始阳光 | 100 |
| 向日葵成本 | 50 |
| 豌豆射手成本 | 100 |
| 向日葵单次产出 | 25 |
| 向日葵产出间隔 | 20–24 秒，默认 22 秒 |
| 阳光自动消失 | 可以设置较长时间；不作为核心机制 |

如果需要简化，阳光可以在一段时间后由轻微吸附动画自动进入阳光计数器，避免堆满屏幕。

## 6.2 植物种植

MVP 的真正可种植物只有：

- 向日葵；
- 豌豆射手。

种植规则：

- 只能落在未被固定对象占据的允许格；
- 一个格子最多一个植物；
- 不实现铲除；
- 不实现植物死亡；
- 植物布局建议持久化，以便桌面花园长期存在。

中路是主要战斗行。为了避免顶部/底部豌豆射手没有目标，MVP 可将“豌豆射手可种区域”限制为中路空格；向日葵可种在其他开放格。以后若扩展多行僵尸，再放开战斗行，不在本版实现。

## 6.3 豌豆射手

只做最小攻击循环：

`发现当前桶装僵尸 → 发射豌豆 → 命中 → 扣除僵尸 HP`

推荐初始调参：

| 参数 | MVP 默认 |
|---|---:|
| 发射间隔 | 1.5 秒 |
| 单发伤害 | 20 |
| 桶装僵尸 HP | 300–400，默认 360 |
| 僵尸死亡后刷新延迟 | 2 秒 |

这些值不是财务规则，可以集中在 `gameBalance` 一处后续微调。

击杀僵尸：

- 不奖励工资；
- 不奖励银币/金币/钻石；
- 默认不奖励额外阳光；
- 不产生积分；
- 不推进关卡。

## 6.4 无限血坚果

坚果逻辑：

- `HP = Infinity`，或者实现层完全不创建可耗尽生命值；
- 桶装僵尸接触后永远进入啃食循环；
- 坚果只播放受击/抖动视觉反馈；
- 不进入“严重破损即将死亡”的永久状态；
- 不会死亡；
- 不会触发失败。

建议动画循环：

`idle → bite reaction → idle`

而不是：

`healthy → damaged → critical → dead`

## 6.5 桶装僵尸

游戏中永远只需要一只“当前活动桶装僵尸”。

状态机：

`spawn_on_street → walk_to_wallnut → bite_wallnut → take_damage → die → respawn`

规则：

- 从右侧街道出现；
- 走向中路坚果；
- 到达后持续啃食；
- 被豌豆击中会掉血；
- 死亡后播放死亡动画；
- 约 2 秒后生成新的桶装僵尸；
- 永远循环。

不要实现：

- 普通僵尸波次；
- 路障僵尸；
- 旗帜僵尸；
- 巨人；
- Boss；
- 多波攻击；
- 关卡；
- 胜利/失败。

---

# 7. 工资计算模型

这是系统最重要的领域逻辑。

## 7.1 术语

- `M`：当期月薪；
- `N`：当期实际工作日数量；
- `D`：每日工资；
- `H`：有效工作小时工资；
- `R`：有效工作秒工资；
- `W(t)`：从当前工作日 08:40 到时刻 `t` 为止累计完成的有效工作秒。

公式：

`D = M / N`

`H = D / 7`

`R = D / 25,200`

## 7.2 当日有效工作秒 W(t)

状态规则：

### 非工作日

`W(t) = 0`

### 08:40 之前

`W(t) = 0`

### 08:40–11:40

`W(t) = t - 08:40`

### 11:40–13:30

`W(t) = 10,800`

因为上午已经完成 3 小时。

### 13:30–17:30

`W(t) = 10,800 + (t - 13:30)`

### 17:30 之后

`W(t) = 25,200`

## 7.3 今日理论已赚

`TodayEarned(t) = W(t) × R`

这必须是高精度计算。

## 7.4 当前工资周期理论已赚

当前周期内：

- 已完整结束的工作日：每个贡献 `D`；
- 当前工作日：贡献 `W(t) × R`；
- 未到来的工作日：贡献 0。

因此完整周期结束时：

`CycleEarned = M`

不应通过把 UI 上显示的两位小数每天相加得到最终金额。

## 7.5 计算模式

禁止把以下方式当作权威账本：

`每秒 setInterval -> balance += R`

正确方式：

> 给定当前时间和周期配置，直接计算此时的理论值。

前端刷新只是“读取/展示”，不是“创造工资”。

这属于确定性派生状态，不是必须依赖连续运行的传统流式累加。

---

# 8. 工作状态状态机

后端应暴露清晰状态：

- `NON_WORKDAY`
- `BEFORE_WORK`
- `WORKING_AM`
- `LUNCH_BREAK`
- `WORKING_PM`
- `AFTER_WORK`

状态只由：

- 当前日期；
- 工作日历；
- 当前本地时间；

确定。

UI 可映射文案，例如：

| 状态 | 文案 |
|---|---|
| BEFORE_WORK | 今日尚未开始 |
| WORKING_AM | 上午工作中 |
| LUNCH_BREAK | 午休中 |
| WORKING_PM | 下午工作中 |
| AFTER_WORK | 今日已完成 |
| NON_WORKDAY | 今日休息 |

午休期间：

- 真实工资不增长；
- 货币应得数量不增长；
- 不产生新的工资货币；
- 已在屏幕上的货币仍可被收集。

---

# 9. 货币事件计算

## 9.1 计数公式

给定某工作日累计有效工作秒 `W`：

### 钻石

`DiamondCount = floor(W / 3600)`

### 金币

`GoldCount = floor(W / 60) - floor(W / 3600)`

### 银币

`SilverCount = floor(W / 10) - floor(W / 60)`

### 总事件

`TotalRewardEvents = floor(W / 10)`

这些公式天然实现：

`钻石覆盖金币，金币覆盖银币`

## 9.2 边界示例

| 时刻 | 有效工作秒 | 银币 | 金币 | 钻石 |
|---|---:|---:|---:|---:|
| 08:40:00 | 0 | 0 | 0 | 0 |
| 08:40:10 | 10 | 1 | 0 | 0 |
| 08:41:00 | 60 | 5 | 1 | 0 |
| 09:40:00 | 3,600 | 300 | 59 | 1 |
| 11:40:00 | 10,800 | 900 | 177 | 3 |
| 13:30:00 | 10,800 | 900 | 177 | 3 |
| 14:30:00 | 14,400 | 1,200 | 236 | 4 |
| 17:30:00 | 25,200 | 2,100 | 413 | 7 |

这里 11:40 到 13:30 计数完全冻结。

下午重新开始后，从上午累计的有效工作秒继续，而不是重新从 0 计时。

---

# 10. 银币 / 金币 / 钻石面值

## 10.1 目标

三种货币应：

- 有明显稀有度和价值差异；
- 面值随当期实际时薪动态换算；
- 完整一个有效工作小时的理论货币总价值必须等于该小时真实工资；
- 完整工作日理论货币总价值必须等于日工资；
- 完整工资周期理论货币总价值必须等于当期月薪；
- 不依赖 UI 四舍五入。

## 10.2 面值权重

冻结权重：

`银币 : 金币 : 钻石 = 1 : 6 : 360`

一个完整有效工作小时：

- 300 银币；
- 59 金币；
- 1 钻石。

加权单位数：

`300×1 + 59×6 + 1×360 = 1014`

设每有效工作小时工资为 `H`。

基础单位：

`B = H / 1014`

那么：

`SilverValue = B`

`GoldValue = 6B`

`DiamondValue = 360B`

校验：

`300B + 59×6B + 360B = 1014B = H`

所以：

- 1 完整有效工作小时：币值总额 = `H`；
- 7 小时完整工作日：币值总额 = `7H = D`；
- N 个完整工作日：币值总额 = `N×D = M`。

## 10.3 重要说明

游戏货币价值不是连续平滑增长的。

因为钻石会在整小时边界一次性贡献较大的价值，所以：

- 真实工资账本会随有效工作秒平滑增长；
- 游戏货币理论价值会按 10 秒事件跳变；
- 游戏收集钱包还会进一步取决于用户何时收集。

因此这两套数值在任意中间时刻不要求相等。

这正是“双系统”的设计目的。

## 10.4 精度

禁止使用 JavaScript `number` 直接长期累加货币金额。

建议：

- Rust 端使用高精度 Decimal；
- SQLite 用文本 Decimal 或其他不会产生二进制浮点误差的方式持久化；
- 前端金额传字符串或精确整型缩放值；
- 内部不把单枚币先四舍五入到 2 位小数；
- 只在 UI 最终展示时格式化为人民币两位小数；
- 账本对账使用未舍入值。

若展示单枚币面值，可显示 3–4 位小数或只显示货币类型，不要求每枚都显示两位人民币。

---

# 11. 实时货币掉落

## 11.1 生成来源

工资货币全部从固定金盏花附近生成。

视觉流程：

`金盏花轻微动作 → 货币出现 → 小幅弹跳/漂浮 → 等待点击或被磁吸`

## 11.2 生成机制

后端不是靠“定时器成功执行一次”决定是否有币。

正确做法：

1. 获取当前应得计数；
2. 与已经“记账/物化”的计数比较；
3. 计算增量；
4. 如果是正常实时增量，生成对应前端动画；
5. 如果跨越时间太长或应用刚恢复，则允许聚合。

因此即使前端掉帧，也不会少发币。

## 11.3 屏幕负载保护

一天理论上有 2,520 个事件，不适合长时间等待手点。

要求：

- 同屏松散工资货币数量设置上限；
- 建议最多保留约 8–12 个；
- 超出后优先触发磁吸；
- 恢复应用时如果积压很多，不要一次创建几百个 Sprite；
- 大量积压使用钱袋聚合。

---

# 12. 磁吸菇自动收集

磁吸菇的产品职责：

> 降低 10 秒级货币产生频率带来的点击负担。

## 12.1 运行中的自动收集

推荐规则：

- 新掉落货币先给用户一个短暂可点击窗口；
- 约 2–3 秒后，如果尚未点击，磁吸菇开始吸附；
- 货币沿曲线飞向磁吸菇/钱包；
- 动画完成后记为已收集；
- 玩家手动点击应立即进入收集流程；
- 同一枚货币只能结算一次。

银币、金币、钻石都可被磁吸。

## 12.2 离线钱袋例外

离线收益钱袋**不应被磁吸菇自动领取**。

钱袋必须保留一个明确的用户点击动作，形成“回来领取离线收益”的仪式感。

---

# 13. 离线 / 休眠 / 重启补偿

这是必须可靠实现的核心功能。

## 13.1 原则

程序不运行时：

- 真实工资仍然可以被重算；
- 应得货币仍然可以被重算；
- 游戏钱包不会凭空自动增加；
- 缺失的货币被包装为“离线收益钱袋”。

## 13.2 启动时对账

应用启动或从长时间挂起恢复时：

1. 计算截至现在理论应得的银/金/钻数量；
2. 查询数据库里已经被“物化、打包或收集”的数量；
3. 计算差额；
4. 将差额创建为一个离线钱袋；
5. 立刻把这些数量标记为“已打包”，避免再次启动重复生成；
6. 钱袋在用户点击前持续存在。

不要用：

`真实工资金额 - 游戏钱包金额`

来推导离线币。

必须比较：

`理论应得货币计数 - 已记账货币计数`

## 13.3 钱袋内容

钱袋至少存：

- 创建时间；
- 所属工资周期；
- 所属日期范围；
- 银币数量；
- 金币数量；
- 钻石数量；
- 精确总价值；
- 是否领取；
- 领取时间。

鼠标悬停可显示：

**离开期间收益**

- 银币 × N；
- 金币 × N；
- 钻石 × N；
- 合计 ¥X.XX。

## 13.4 钱袋领取动画

点击：

`钱袋打开 → 少量代表性银币/金币/钻石飞出 → 飞向钱包 → 钱包数字增加`

不要真的为 500 枚币创建 500 个 Sprite。

动画只需要代表性数量，结算必须一次性按真实袋内总数完成。

## 13.5 幂等性

关键验收：

- 创建钱袋后立即关闭应用；
- 再次启动；
- 原钱袋仍在；
- 不能再生成一份相同差额；
- 连续启动 10 次也不能重复；
- 钱袋领取请求重复触发时只结算一次。

---

# 14. 跨工资周期

如果上一个工资周期结束时还有未收集货币：

- 不清零；
- 不计入新周期的“应得生成”；
- 仍然保留其原周期归属；
- 可以显示为“上周期未领取”钱袋；
- 用户领取后进入总收集账本，但统计时仍可追溯原周期。

新的工资周期：

- 创建新的工资周期快照；
- 重新计算工作日数量；
- 重新计算 `D / H / R`；
- 重新计算银币、金币、钻石面值；
- 不修改历史周期的面值和结算记录。

---

# 15. 工资周期快照

为了避免后续配置变更把历史账本改坏，每个工资周期建立快照。

建议快照包含：

| 字段 | 含义 |
|---|---|
| cycle_id | 周期 ID |
| start_date | 周期首工作日 |
| end_date | 周期末工作日 |
| salary_monthly | 当期月薪 |
| workday_count | 实际工作日数量 |
| daily_pay | 日工资 |
| hourly_pay | 有效小时工资 |
| per_second_pay | 有效秒工资 |
| silver_value | 银币精确面值 |
| gold_value | 金币精确面值 |
| diamond_value | 钻石精确面值 |
| timezone | 计算时区 |
| calendar_version | 工作日历版本 |
| created_at | 快照时间 |

当前版本原则：

> 同一个工资周期的计薪参数稳定不变。

用户修改“月薪”时，默认应用到下一个新周期。不要静默重写已经进行中的周期历史。

如果以后要支持“当前周期重新计算”，应做成显式高级操作，不属于 MVP。

---

# 16. 游戏钱包

## 16.1 游戏钱包增加条件

只有两类动作能增加：

1. 实时货币被手动点击或磁吸菇完成收集；
2. 离线钱袋被用户点击领取。

货币“生成/应得”本身不能直接增加 collected wallet。

## 16.2 统计口径

至少维护：

- 今日已收集；
- 当前工资周期已收集；
- 历史总收集（可以持久化，但 MVP UI 不一定展示）；
- 当前待收集；
- 当前未领取钱袋总额。

## 16.3 收集事务

一次收集应该是原子操作：

`待收集/已物化 → 已收集`

不能出现：

- UI 动画播放了但数据库没记；
- 数据库记了两次；
- 点击与磁吸同时命中造成双结算。

建议每个实时可收集对象有唯一 ID；钱袋也有唯一 ID。

---

# 17. 推荐的数据持久化模型

不需要为每天 2,520 次事件逐条永久保存。

推荐使用“确定性计数 + 汇总状态”。

## 17.1 app_settings

保存：

- wallet_display_mode；
- sound_enabled；
- auto_collect_enabled；
- full_window_size；
- collapsed_window_size；
- last_window_position；
- autostart（可选）；
- always_on_top（可选）；
- timezone；
- 当前用户输入的下一周期月薪；
- 节假日配置版本。

## 17.2 payroll_cycles

保存第 15 节的周期快照。

## 17.3 daily_reward_state

每个工作日一条。

建议字段：

- date；
- cycle_id；
- entitled_silver；
- entitled_gold；
- entitled_diamond；
- accounted_silver；
- accounted_gold；
- accounted_diamond；
- collected_silver；
- collected_gold；
- collected_diamond；
- collected_value_exact；
- updated_at。

解释：

- `entitled` 可以重算，不必以 DB 为真值；
- `accounted` 表示已经作为实时对象、钱袋或已领取记录进入系统的数量；
- `collected` 表示真正进入钱包的数量。

## 17.4 reward_bags

保存离线钱袋。

## 17.5 collection_ledger

记录实际钱包变更。

类型可包括：

- `LIVE_COIN_COLLECT`;
- `OFFLINE_BAG_CLAIM`.

建议存：

- transaction_id；
- cycle_id；
- work_date；
- source_id；
- silver_count；
- gold_count；
- diamond_count；
- exact_value；
- created_at。

这是审计层。

## 17.6 game_state

保存与工资无关的轻游戏状态：

- 阳光余额；
- 已种植物位置；
- 植物类型；
- 当前僵尸状态如果需要；
- 当前窗口游戏设置。

工资账本与 `game_state` 不应交叉修改。

---

# 18. 后端与前端职责边界

## 18.1 Rust / Tauri 后端负责

后端是权威层，负责：

- 工资周期计算；
- 工作日判断；
- 有效工作秒；
- 真实工资；
- 应得货币计数；
- 货币面值；
- 周期快照；
- SQLite；
- 离线差额；
- 钱袋创建；
- 钱袋领取；
- 钱包原子结算；
- 防重复；
- 输出可供 UI 使用的 snapshot。

## 18.2 React 负责

React 负责：

- 页面框架；
- 设置；
- 钱包 UI；
- 当前工作状态；
- 植物卡；
- 弹窗 / Tooltip；
- 模式切换；
- 与 Tauri command/event 通信。

React 不应自行成为工资真值来源。

## 18.3 PixiJS 负责

PixiJS 负责：

- 草坪渲染；
- 房屋/街道；
- 植物；
- 僵尸；
- 豌豆；
- 阳光；
- 工资货币；
- 磁吸动画；
- 钱袋；
- 粒子和动效。

PixiJS ticker 只能驱动**视觉动画**。

禁止使用 Pixi ticker 的累计 `delta` 作为工资或货币应得的来源。

---

# 19. 前后端同步策略

推荐后端对前端提供一个统一状态快照。

快照应包含至少：

- 当前时间；
- 当前工资周期；
- 当前工作状态；
- 今日有效工作秒；
- 今日真实已赚；
- 周期真实已赚；
- 今日理论应得银/金/钻；
- 周期理论应得银/金/钻；
- 今日已收集；
- 周期已收集；
- 待收集价值；
- 待领取钱袋；
- 当前三种货币面值。

前端：

- 可以每 500ms–1s 请求/接收工资 snapshot；
- 实时工资数字可做平滑视觉插值；
- 每次后端 snapshot 到达必须进行校准；
- coin spawn 通过“计数增量”触发，而不是依赖前端自己的 10 秒 timer；
- 应用从后台恢复时立即 full reconcile。

---

# 20. 系统时间与恢复

## 20.1 正常情况

以配置时区下的系统时间作为个人工资可视化依据。

## 20.2 系统休眠

恢复后：

- 不补跑几千个 timer callback；
- 直接重新计算当前时间；
- 真实工资跳到正确值；
- 应得币进行差额对账；
- 大差额聚合成钱袋。

## 20.3 系统时钟回拨

本产品不是防作弊工资系统，不需要复杂反篡改。

但必须保证：

- 已经领取的钱不能因为时间回拨被扣除；
- 如果当前理论应得数小于已经 accounted 的数，暂时不生成新币；
- 等理论时间追上后再继续；
- 不产生负数差额；
- 不反向删除历史 ledger。

---

# 21. PVZ 资源集成

用户计划使用：

`https://github.com/qingzhixing/PVZ-Resources-Sorted`

仓库资源来自原游戏提取整理。

已确认这类资源结构包含：

- `Sprites`;
- `SFX`;
- 金盏花；
- 磁吸菇；
- 豌豆射手；
- 向日葵；
- 坚果；
- 桶装僵尸；
- 三行草坪相关资源；
- 投射物等资源。

## 21.1 资源使用原则

不要在业务代码中散落硬编码文件路径。

建立统一 asset manifest，将逻辑角色映射到用户本地资源：

| 逻辑角色 | 资产键示例 |
|---|---|
| marigold | plant.marigold |
| magnetshroom | plant.magnet |
| sunflower | plant.sunflower |
| peashooter | plant.peashooter |
| wallnut | plant.wallnut |
| bucket_zombie | zombie.bucket |
| pea | projectile.pea |
| lawn_3row | scene.lawn3 |
| coin_silver | reward.silver |
| coin_gold | reward.gold |
| diamond | reward.diamond |
| bag | reward.offlineBag |

Codex 应先扫描实际下载目录再建立清单，不要仅凭猜测文件名。

## 21.2 Reanimation 资源

资源包中的植物/僵尸可能是：

- 头；
- 嘴；
- 花瓣；
- 眼睛；
- 身体；
- 其他局部 PNG；

而不一定是完整逐帧 SpriteSheet。

因此渲染层要支持：

`多个组件 Sprite → Container 组合 → 局部位移/旋转/缩放 → 简单循环`

MVP 不要求 1:1 还原原游戏 Reanimation 时间轴。

如果某角色无法完整重建：

1. 优先用组合部件；
2. 其次使用静态完整图；
3. 再加轻微呼吸、摆动、眨眼；
4. 不阻塞工资系统开发。

## 21.3 授权注意

该资源仓库本身未明确提供可用于重新分发原游戏美术的许可证。

因此工程上应：

- 把 PVZ 原始资源视为用户本地外部资产；
- 不把授权状态写死成“可商用”；
- 不在没有进一步权利确认的情况下自动对外发布/分发这些原版资产；
- 资产层必须可替换，以便未来替换为原创同类视觉。

这不影响本地原型和代码架构开发，但影响未来发行决策。

---

# 22. 视觉层级

建议 Pixi 场景层级从后到前：

1. `background_house_street`
2. `lawn`
3. `grid_overlay_optional`
4. `back_row_plants`
5. `main_row_fixed_plants`
6. `player_plants`
7. `zombie`
8. `projectiles`
9. `salary_rewards`
10. `sun_rewards`
11. `effects`
12. `floating_ui`

React DOM UI 可覆盖在 Pixi canvas 外层：

- 顶部卡片；
- 钱包；
- 设置；
- Tooltip；
- 模式切换。

---

# 23. 动画设计

## 23.1 金盏花

至少：

- idle 呼吸；
- 眨眼；
- 产币时轻微弹跳；
- 产钻石时可稍强化反馈。

## 23.2 货币

银币：

- 小幅弹起；
- 旋转；
- 光泽较低。

金币：

- 更明显弹跳；
- 更亮；
- 提示音更清楚。

钻石：

- 明显闪光；
- 缩放入场；
- 轻微粒子；
- 不要过长打断工作。

## 23.3 磁吸

建议：

`coin idle → magnet activates → coin accelerates → curved flight → wallet pulse`

结算最好在动画进入不可逆阶段或结束时以唯一 transaction 完成，并保证重复动画事件不会重复记账。

## 23.4 僵尸

只需：

- walk；
- bite；
- hit；
- death。

## 23.5 坚果

只需：

- idle；
- bite reaction。

## 23.6 钱袋

- 初次出现有轻微落地/弹跳；
- 悬停有提示；
- 点击开袋；
- 代表性货币飞向钱包；
- 钱包金额跳增。

---

# 24. 声音

如资源包已有 SFX，可映射：

- coin drop；
- coin collect；
- diamond；
- sunflower sun；
- pea fire；
- zombie hit；
- zombie bite；
- zombie death；
- bag open。

设置提供总开关。

声音不应每 10 秒都非常刺耳。银币声音可以更轻；金币和钻石更突出。

---

# 25. 不做的功能

当前版本明确不做：

- 完整 PVZ；
- 关卡；
- 波次；
- 胜利；
- 失败；
- 多种僵尸；
- 多种攻击植物；
- 坚果可死亡；
- 植物可死亡；
- 植物升级；
- 商店；
- 成就；
- 登录账号；
- 云同步；
- 排行榜；
- 社交；
- 金钱提现；
- 真实银行连接；
- 杀僵尸增加工资；
- 阳光兑换工资；
- 游戏资源影响真实工资；
- 后端服务器；
- 复杂反作弊；
- 依赖持续运行才能保证工资正确。

Codex 不应“顺手”补这些功能。

---

# 26. 设置页

MVP 设置至少包括：

## 工资

- 月薪（应用到下一工资周期）；
- 当前周期信息只读；
- 当前周期实际工作日数；
- 当前日工资/小时工资只读；
- 三种货币当前周期面值只读。

## 日历

- 时区；
- 法定节假日列表；
- 当前规则提示：“周末不计薪”。

## 显示

- 显示口径：实时工资 / 游戏钱包；
- 完整/收起模式；
- 始终置顶：可选；
- 开机启动：可选。

## 游戏

- 磁吸自动收集开关，默认开；
- 音效开关；
- 动画强度后续可选。

不要把核心计薪时段做成容易误触的普通 UI 项。当前 08:40–11:40 / 13:30–17:30 视为产品规则。

---

# 27. 推荐项目目录职责

以下仅表示职责，不要求完全照搬目录名：

- `src-tauri/domain/payroll`：工资周期、工作日、有效秒、真实金额；
- `src-tauri/domain/rewards`：币计数、面值、离线差额；
- `src-tauri/domain/wallet`：收集事务；
- `src-tauri/persistence`：SQLite；
- `src-tauri/commands`：对 React 暴露的命令；
- `src/app`：React 应用壳；
- `src/features/wallet`：钱包 UI；
- `src/features/settings`：设置；
- `src/game`：Pixi 初始化和主循环；
- `src/game/scenes`：草坪；
- `src/game/entities`：植物、僵尸、投射物、货币、钱袋；
- `src/game/assets`：asset manifest / loader；
- `src/shared/types`：前后端 DTO 类型；
- `assets/pvz`：用户提供的本地资源入口。

核心原则：

> 领域计算与 Pixi 渲染彻底分离。

---

# 28. Codex 推荐实现顺序

## Phase 1 — 工资内核

先不加载 PVZ 素材。

完成：

- 工资周期；
- 工作日历；
- W(t)；
- 今日工资；
- 周期工资；
- 高精度金额；
- 单元测试。

只有这一层全部通过后进入下一阶段。

## Phase 2 — Reward Engine

完成：

- S/G/D 计数；
- 覆盖规则；
- 面值 1:6:360；
- 小时/天/周期金额校验；
- 理论应得 snapshot。

## Phase 3 — SQLite 与离线对账

完成：

- 周期快照；
- daily_reward_state；
- collection ledger；
- reward bags；
- 启动 reconcile；
- 幂等测试。

## Phase 3.5 — Tauri Application API / Frontend Bridge

完成：

- 统一 AppSnapshot；
- Tauri command API；
- Rust / TypeScript DTO；
- React 只读后端权威数据；
- 最小开发调试区；
- command / application service 测试。

## Phase 4A — Salary Configuration & Calendar Application API

完成：

- 首次工资配置；
- 当前周期与下一周期工资语义；
- 正式工资周期快照初始化；
- 月份日历查询；
- 工资配置与日历 Tauri commands；
- 最小配置 / 日历调试区。

## Phase 4B.1 — React Product UI Architecture & Functional UI

完成：

- 今日/周期钱包；
- 显示口径切换；
- 工作状态；
- 月份日历网格；
- 设置；
- 离线钱袋领取；
- PixiJS 稳定占位容器；
- Loading / Error / Retry；
- React 功能测试。

## Phase 4B.2 — PVZ Visual Skin

在不改变 Phase 4B.1 功能边界和财务逻辑的前提下，完成：

- 扫描用户本地 PVZ resources 并建立集中式 UI asset manifest；
- Seed Bank / 四植物卡片静态皮肤；
- Dashboard / Wallet / Work Status HUD 皮肤；
- Calendar / Settings / Offline Bag 皮肤；
- GameScenePlaceholder 静态草坪背景；
- Loading / Error / Retry 统一皮肤；
- 按 feature 拆分 CSS；
- 保持 HTML 文本、输入与可访问控件；
- 不引入 PixiJS、动画实体或游戏循环。

## Phase 5A — PixiJS Core Scene & Lawn Coordinate System

已完成：

- 引入 PixiJS；
- 固定逻辑场景与 contain/letterbox；
- 0-based `3 × 7` 草坪坐标系；
- `getCellCenter(row, column)`；
- background / grid / plant / zombie / projectile / reward / effect / debug layers；
- 中路固定 Marigold / Magnet-shroom / Wall-nut；
- Bucket Zombie 从道路进入并停在 Wall-nut 前；
- React/Pixi 生命周期与 resize；
- Pixi ticker 仅用于视觉。

## Phase 5B — Live Salary Reward Materialization & Settlement

已完成：

- `live_reward_events` migration；
- 确定性唯一键 `cycle_id + work_date + event_index`；
- `PENDING / COLLECTED / PACKAGED`；
- `sync_live_rewards`；
- `list_pending_live_rewards`；
- `collect_live_reward`；
- 同屏 PENDING 上限 12；
- Manual / Magnet 共用同一事务结算；
- restart stale pending → Offline Bag；
- settlement 幂等；
- Pixi rewardLayer 的 Silver / Gold / Diamond 点击收集。

实现说明：

`live_reward_events` 用于已经物化的可领取实体及其结算身份，它不是 Reward Entitlement 真值来源。Reward Entitlement 仍由有效工作秒确定性重算。

## Phase 5C — Reward Presentation, Magnet Auto-Collection & Effects

已完成：

- `SPAWNING → IDLE → MAGNETIZING → SETTLING → REMOVED`；
- Marigold 附近弹出 / 下落；
- 12 个确定性视觉落点；
- Silver / Gold / Diamond 差异化表现；
- Auto Collect 开启后约 2.5 秒 linger；
- oldest-first Magnet queue；
- Manual / Magnet 共享 in-flight settlement；
- settlement 成功后才移除；
- command error 恢复 IDLE；
- sprite pool；
- scene unmount 清理 ticker / listener / entity；
- Offline Bag 不参与 Magnet 自动领取。

## Phase 6 — PVZ Character Animation & Scene Polish

下一阶段：

- Marigold 组合部件 / idle / blink / production reaction；
- Magnet-shroom idle / attracting feedback；
- Wall-nut idle / bite reaction；
- Bucket Zombie walk / bite；
- anchor / scale / shadow / scene depth；
- 生命周期与性能；
- 不修改 Phase 5B / 5C 的金融结算语义。

## Phase 7A — Sun Economy + Sunflower + Planting

计划：

- 阳光；
- 向日葵产阳光；
- 向日葵 / 豌豆射手种植；
- 阳光成本；
- 格子占用；
- game state persistence。

工资与阳光完全隔离。

## Phase 7B — Peashooter + Zombie Combat

计划：

- 豌豆射击；
- projectile collision；
- Zombie HP；
- hit / death；
- 约 2 秒 respawn；
- Wall-nut 永生。

## Phase 8 — 桌面壳打磨

计划：

- 收起模式；
- 窗口位置；
- 可选 always-on-top；
- 可选 autostart；
- 音量；
- 性能；
- 打包。

---

# 29. 必须通过的验收用例

## 29.1 工资时间

### Case A：上班前

工作日 08:39:59：

- W = 0；
- 今日真实工资 = 0；
- S/G/D = 0。

### Case B：第一个银币

08:40:10：

- W = 10；
- 银币 = 1；
- 金币 = 0；
- 钻石 = 0。

### Case C：第一分钟

08:41:00：

- W = 60；
- 银币 = 5；
- 金币 = 1；
- 钻石 = 0。

说明 60 秒位置只有金币，不额外生成银币。

### Case D：第一小时

09:40:00：

- W = 3,600；
- 银币 = 300；
- 金币 = 59；
- 钻石 = 1；
- 理论货币价值 = H；
- 真实工资 = H。

### Case E：上午结束

11:40:00：

- W = 10,800；
- S = 900；
- G = 177；
- D = 3；
- 真实工资 = 3H。

### Case F：午休

12:30：

结果与 11:40 完全一致。

### Case G：下午刚开始

13:30：

结果仍与 11:40 一致。

13:30:10 才产生下午第一个新事件。

### Case H：全天结束

17:30：

- W = 25,200；
- S = 2,100；
- G = 413；
- D = 7；
- 理论货币总值 = D（日工资）；
- 今日真实工资 = D（日工资）。

### Case I：非工作日

任意周末/节假日：

- W = 0；
- 不新增工资；
- 不新增工资货币。

## 29.2 精度

完整工资周期最后一个工作日 17:30：

- 真实工资精确总额 = M；
- 当期理论货币总价值精确总额 = M；
- 两者不因单枚货币 UI 四舍五入产生分差。

## 29.3 离线

场景：

1. 10:00 关闭；
2. 14:20 再打开。

预期：

- 真实工资直接显示 14:20 的正确值；
- 根据有效工作秒重算缺失 S/G/D；
- 生成一个离线钱袋；
- 不把数百个币全部画在草坪上。

## 29.4 钱袋幂等

创建钱袋但未领取：

- 重启 1 次：仍为同一个袋；
- 重启 10 次：仍只有同一份收益；
- 领取一次后重启：不再出现；
- 双击领取：只结算一次。

## 29.5 钱包切换

切换：

`实时工资 ↔ 游戏钱包`

不得：

- 修改真实账本；
- 自动领取币；
- 创建新币；
- 改变当前钱袋；
- 改变植物状态。

## 29.6 磁吸与点击竞争

同一枚金币：

- 玩家点击的同时磁吸开始；
- 最终 collection ledger 只能有一笔；
- 钱包只能增加一次。

## 29.7 休眠

应用运行时系统休眠 90 分钟后恢复：

- 不依赖 90 分钟的 timer callback；
- 立即重算；
- 正确处理午休边界；
- 根据差额发放/打包；
- 不崩溃；
- 不生成巨量 Sprite。

---

# 30. 性能要求

本应用应长期挂在桌面。

目标：

- 非工作时段尽量低 CPU；
- Pixi 动画合理限帧；
- 窗口收起后降低渲染频率；
- 工资计算不需要高频重型 SQL；
- 不保存数百万条 10 秒事件；
- 货币使用对象池或轻量 Sprite；
- 同屏实体数量受控；
- 大批量收益使用聚合钱袋。

工资计算本身是纯函数级别轻量操作，可以高频读取，但 UI 没必要每帧请求 Rust。

---

# 31. 安全与数据原则

这是本地个人工具。

- 工资数据默认只存在本机；
- 不上传工资；
- 不需要账户；
- 不需要遥测才能运行；
- 数据库异常时优先保护账本与历史周期；
- 所有钱包写入操作事务化；
- 能通过确定性公式重建的状态尽量不要冗余保存。

---

# 32. 最重要的领域不变量

Codex 在重构时必须始终保持：

**Invariant 1**

完整工资周期真实工资：

`CycleEarned(end) = MonthlySalary`

**Invariant 2**

一个完整有效工作小时：

`300 Silver + 59 Gold + 1 Diamond`

**Invariant 3**

完整小时理论货币价值：

`CoinEntitlementValue(1h) = HourlyPay`

**Invariant 4**

完整工作日：

`CoinEntitlementValue(7h) = DailyPay`

**Invariant 5**

完整工资周期：

`CoinEntitlementValue(cycle) = MonthlySalary`

**Invariant 6**

真实工资不依赖应用持续运行。

**Invariant 7**

游戏钱包只因真实“收集事务”增长。

**Invariant 8**

离线差额基于货币 entitlement count，而不是基于金额相减。

**Invariant 9**

任何货币或钱袋最多结算一次。

**Invariant 10**

午休、周末、节假日不产生工资，也不产生工资货币。

**Invariant 11**

阳光、植物、僵尸不会改变工资。

**Invariant 12**

坚果永远不会死亡。

---

# 33. 产品决策优先级

如果实现过程中出现冲突，按以下优先级判断：

1. 工资准确性；
2. 离线/重启后可恢复性；
3. 钱包与账本不重复结算；
4. 用户不会因为应用没运行而损失应得收益；
5. PVZ 视觉氛围；
6. 游戏互动；
7. 动画还原度。

永远不要为了动画“更像 PVZ”牺牲账本确定性。

---

# 34. 给 Codex 的执行指令

在开始编码前，Codex 应完整阅读本文，并把以下内容当作约束：

- 不擅自扩展完整 PVZ 玩法；
- 不新增僵尸、植物、波次或关卡；
- 不把工资计算写在 React/Pixi ticker 中；
- 不依赖 setInterval 累加余额；
- 不把 UI 两位小数当会计值；
- 不逐条永久保存每 10 秒 reward event；
- 不把游戏钱包当真实工资；
- 不用“真实工资 - 已收集金额”计算离线补发；
- 不允许钱袋重复领取；
- 不允许磁吸和点击双结算；
- 不让坚果死亡；
- 不让僵尸击杀奖励工资；
- 不让阳光进入工资体系；
- 不要求应用常驻才能正确计薪；
- 不在业务逻辑中硬编码用户本地 PVZ 文件名；
- 不因为某个美术资源缺失而阻塞工资系统；
- 原版 PVZ 资源视为可替换的外部资产层。

Codex 应优先创建领域层单元测试，然后再做 Pixi 美术。

---

# 35. MVP 完成定义（Definition of Done）

只有以下全部满足，才视为 MVP 完成：

1. 输入月薪后能创建正确工资周期；
2. 能正确计算工作日数；
3. 08:40–11:40 / 13:30–17:30 计薪正确；
4. 午休不计薪；
5. 周末/法定节假日不计薪；
6. 今日/周期真实工资正确；
7. 10s / 60s / 3600s 币种覆盖正确；
8. S/G/D 面值动态随周期工资计算；
9. 周期结束币值总额严格等于月薪；
10. 游戏钱包与真实工资独立；
11. 实时币可手点；
12. 磁吸菇可自动收；
13. 应用未运行期间收益不会丢；
14. 重启能生成离线钱袋；
15. 钱袋可点击一次性领取；
16. 钱袋重复启动不复制；
17. 三行草坪、左房子、右街道成立；
18. 顶部只有四张植物卡；
19. 金盏花、磁吸菇固定；
20. 无限血坚果成立；
21. 桶装僵尸持续啃坚果；
22. 向日葵产生阳光；
23. 豌豆射手能消耗阳光种植并攻击；
24. 僵尸死亡后会刷新；
25. 无关卡/波次/失败；
26. 资源层可以加载用户下载的 PVZ 素材；
27. 资源缺失时有占位/静态回退，不影响账本；
28. 完整模式和收起模式可用；
29. 钱包口径切换可用；
30. 长期运行无明显内存/实体无限增长。

---

# 36. 最终产品模型

整个系统可以压缩成两个互不污染的循环。

## 工资循环

`工作时间`
→ `真实工资可重算`
→ `货币 entitlement 可重算`
→ `金盏花视觉掉落`
→ `点击 / 磁吸 / 离线钱袋`
→ `游戏收集钱包`

## 轻游戏循环

`向日葵`
→ `阳光`
→ `种豌豆射手`
→ `攻击桶装僵尸`
→ `僵尸死亡`
→ `新桶装僵尸`
→ `继续啃无限血坚果`

这两个循环只在视觉场景中共存，**经济逻辑绝不互相影响**。

---

# 37. 最后强调

这个应用的核心不是“做一个缩小版植物大战僵尸”。

核心是：

> 用一个有生命感的 PVZ 风格桌面场景，把本来抽象、缓慢、无反馈的上班收入变成可看见、可收集、不会丢失的实时反馈。

所以实现时：

- **工资账本必须严谨；**
- **货币账本必须可重算；**
- **离线补偿必须幂等；**
- **游戏玩法必须克制；**
- **视觉体验可以丰富，但不能反过来绑架财务逻辑。**

到这一版为止，产品边界已足够明确，可以正式开始编码。


---

# 38. 当前实现状态（2026-08-18）

当前代码已完成至 **Phase 5C**。

## 38.1 已完成阶段

- Phase 1 — Payroll Engine；
- Phase 1.5 — Tauri Desktop Shell；
- Phase 2 — Deterministic Reward Engine；
- Phase 3 — SQLite / Ledger / Offline Reconciliation；
- Phase 3.5 — Tauri Application API / Frontend Bridge；
- Phase 4A — Salary Configuration & Calendar API；
- Phase 4B.1 — React Product UI；
- Phase 4B.2 — PVZ Visual Skin；
- Phase 5A — PixiJS Core Scene；
- Phase 5B — Live Reward Materialization & Settlement；
- Phase 5C — Reward Presentation / Magnet Auto Collection。

下一阶段：

> **Phase 6 — PVZ Character Animation & Scene Polish**

## 38.2 当前自动化验证基线

截至 Phase 5C：

- Rust：`64/64 passed`；
- Frontend：`34/34 passed`；
- `cargo fmt --check`：通过；
- `cargo clippy --all-targets -- -D warnings`：通过；
- `npm run build`：通过；
- `git diff --check`：通过；
- `npm run tauri dev`：Windows 真实窗口验证通过。

## 38.3 当前真实窗口已经具备

- PVZ 风格 Dashboard；
- Salary / Game Wallet；
- Calendar；
- Settings；
- Offline Reward Bag；
- PixiJS 草坪；
- 3×7 logical lawn；
- Marigold；
- Magnet-shroom；
- Wall-nut；
- Bucket Zombie；
- Silver / Gold / Diamond live rewards；
- Manual click collection；
- Magnet auto collection；
- Rust transaction 后钱包刷新；
- scene mount/unmount 安全。

## 38.4 当前继续开发入口

下一阶段只做 Phase 6 的角色动画与场景精修。

Phase 6 不应修改：

- Payroll Truth；
- Reward Entitlement；
- `live_reward_events` 业务唯一性；
- `accounted / collected`；
- Offline reconciliation；
- `collect_live_reward(event_id)` 的事务语义。

---

# 39. 新开发者交接规则

第一次拉取仓库时：

1. 先阅读根目录 `README.md`；
2. 再完整阅读本 SSOT；
3. 执行前后端全部测试；
4. 用 `npm run tauri dev` 启动真实 Windows 应用；
5. 确认当前 Phase 5C 行为正常；
6. 从 Phase 6 开始继续。

必须继续遵守：

> Rust 是工资、Reward、Wallet 和 SQLite 的权威层；React 只展示并调用 command；PixiJS 只负责视觉世界。

尤其禁止：

- 用 Pixi ticker 产生工资；
- 用 JavaScript `number` 做账本累计；
- 让 Magnet / Manual click 分别实现两套 settlement；
- 绕过 `collect_live_reward(event_id)` 直接改钱包；
- 用 `Real Salary - Game Wallet` 算离线收益；
- 为了动画效果修改已经冻结的工资不变量。
